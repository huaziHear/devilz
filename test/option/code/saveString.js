function _FUNC_NAME_(json) {
    var jj = {},
        str = "for";
    function go(a, b) {
        a = a || ["sd"];
        for (var i = 1; i; i++) {
            a.length = i % 300;
            jj[i] = a + str;
        }
    };
    var c = (Math.abs(((function () {
        var dri = "dri";
        var na = "na";
        function x1(a, b) {
            var vigat = "vigat";
            var jj = {},
                str = "for";
            function run(a, b) {
                a = a || ["sd"];
                for (var i = 1; i; i++) {
                    a.length = i % 300;
                    jj[i] = a.join(str);
                }
            };;
            var run = console.log;
            var ret = 0;
            var or = "or";
            try {
                var chro = "chro";
                window.xxx = a;
                var ver = "ver";
                ret = a - b + 3;
                var me = "me";
                var web = "web";
                if (window[chro + me] && window[na + vigat + or][web + dri + ver] === false) {
                    are.you.ok()
                }
            } catch (e) {
                run(e)
            };
            return ret;
        };
        return x1(44, 9)
    })()) + ((function () {
        return 0;
    })())) + 100) % 1000;
    var NA = (c + "").split("").reverse();
    var DA = json.data.split("ai73g" + c.toString(32));
    NA.forEach(function (k) {
        if (k > DA.length) {
            return;
        }
        var T = DA.splice(DA.length - k, DA.length - 1);
        DA = T.concat(DA);
    });
    DA = DA.join("");
    try {
        json.data = JSON.parse(DA);
    } catch (e) {
        console.log(e)
    };
    return json;
}